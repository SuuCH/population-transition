import type { VFC } from "react";

const Header: VFC = () => {
  return (
    <header
      style={{ color: "white", fontSize: "2.0rem", backgroundColor: "black" }}
    >
      PopulationTransition
    </header>
  );
};

export { Header };
